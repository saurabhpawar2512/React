import React from "react";
import ReactDOM from "react-dom";

import App from "./Containers/App";

const rootElement = document.getElementById("root");
ReactDOM.render(<App title="People Manager" />, rootElement);
